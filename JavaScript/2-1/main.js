// 「myfunc」という関数を用意。
function myfunc() {
    // idが「box」の要素を取得して、変数changeに代入
    let change = document.getElementById("box");
    // change.style.backgroundColorを使って色を書き変える
    change.style.backgroundColor = "red";
}